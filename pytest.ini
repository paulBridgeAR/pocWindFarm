[pytest]
# transforms.py lives next to the pipeline files and data_generator.py in tools,
# so both go on the path. A plain `pytest` from the repo root then just works.
pythonpath = pipeline tools
testpaths = tests
